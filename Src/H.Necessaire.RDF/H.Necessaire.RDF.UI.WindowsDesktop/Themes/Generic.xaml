<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:H.Necessaire.RDF.UI.WindowsDesktop"
    xmlns:chrome="using:H.Necessaire.RDF.UI.WindowsDesktop.Chromes">

    <Style TargetType="chrome:DefaultWindowChrome" >
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="chrome:DefaultWindowChrome">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
